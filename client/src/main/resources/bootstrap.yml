spring:
  # 应用名称
  application.name: foo
  cloud:
    config:
      env: default
      # 可选项
      # profile: development
      label: master
      # 配置config-server暴露的获取配置接口,其默认值为http://localhost:8888
      uri: http://localhost:8888